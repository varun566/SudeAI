# Used when running the API outside Docker Compose.
DATABASE_URL=postgresql+asyncpg://telemetry:telemetry@localhost:5432/telemetry
ENVIRONMENT=development
LOG_LEVEL=INFO
# Comma-separated. Leave blank to disable CORS middleware.
CORS_ORIGINS=http://localhost:3000
